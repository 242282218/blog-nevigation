# 分阶段优化路线图

生成日期：2026-06-07
目标：把审查结论转成可交给后续 coding agent 的分阶段任务。默认不改业务方向：继续 Next.js、自带后台、本地文件持久化、R2 明文备份、Docker/GHCR 发布。

## 阶段划分原则

- 第 0 阶段：防止硬约束回归，立即修文档和测试护栏。
- 第 1 阶段：发布前必须修复的工程风险。
- 第 2 阶段：后台 UX 和可访问性。
- 第 3 阶段：数据架构一致性。
- 第 4 阶段：长期演进，按数据规模和使用场景触发。

## 第 0 阶段：立即修复

### 0.1 固化 R2 明文备份规则

目标：任何后续修改都不能把 R2 远端备份改回口令式方案。

收益：避免再次出现用户无法直接检查/恢复 R2 JSON 的回归。

风险：无业务风险。

涉及文件：

- `AGENTS.md`
- `README.md`
- `tests/lib/r2-backup-storage.test.ts`
- `tests/app/setup-route.test.ts`
- `tests/app/cloudflare-r2-route.test.ts`

验证方式：

- 检查 docs、src、README、deploy 中没有已移除的远端备份口令设计相关文案。
- `npm run test:architecture`
- `npm run test -- tests/lib/r2-backup-storage.test.ts tests/app/setup-route.test.ts tests/app/cloudflare-r2-route.test.ts`

预计工作量：0.5 天。

### 0.2 固化 Docker 更新保留 `.env data`

目标：更新镜像不覆盖生产配置和数据。

收益：用户更新后不需要重新配置，不丢数据。

风险：脚本测试需要 fake docker/git/tar，注意不要真实操作本机 Docker。

涉及文件：

- `README.md`
- `compose.yaml`
- `deploy/compose.prod.yaml`
- `deploy/git-deploy.sh`
- `.github/workflows/docker-deploy.yml`
- `tests/architecture/repository-structure.test.ts`

验证方式：

- 静态断言 bind mount。
- 新增 `tests/scripts/docker-update.test.ts` 行为级测试。
- `npm run test:architecture`。

预计工作量：1 天。

### 0.3 文档索引更新

目标：让后续 agent 能直接从中文文档进入任务。

收益：减少重复审查。

风险：无。

涉及文件：

- `docs/优化文档索引.md`
- 本次 8 份优化文档。

验证方式：

- `git diff --check`
- docs 链接人工快速检查。

预计工作量：0.5 天。

## 第 1 阶段：发布前修复

### 1.1 Docker 同源镜像 smoke

目标：测试镜像和发布镜像使用同一 Buildx 构建产物/缓存。

收益：消除“测的不是发的”。

风险：workflow 改动较大，需要一次只改 Docker job。

涉及文件：

- `.github/workflows/docker-deploy.yml`
- `.github/workflows/ui-smoke.yml`
- `scripts/test/*.py`

验证方式：

- GitHub Actions 运行成功。
- Docker smoke 日志显示挂载临时数据目录。
- GHCR digest 输出。

预计工作量：1.5 天。

### 1.2 Docker smoke 挂载真实数据目录

目标：CI 中验证容器对 `/var/lib/blog-navigation` 的读写和重启保留。

收益：提前发现权限、entrypoint、volume 问题。

风险：需要清理临时容器和目录。

涉及文件：

- `.github/workflows/docker-deploy.yml`
- `Dockerfile`
- `deploy/docker-entrypoint.sh`

验证方式：

- CI docker run 包含 `-v "$PWD/.tmp/docker-smoke-data:/var/lib/blog-navigation"`。
- 容器重启后 marker 仍存在。

预计工作量：1 天。

### 1.3 digest 部署与 `.last-good-digest`

目标：生产部署和回滚使用不可变 digest。

收益：回滚目标确定，避免 `latest` 漂移。

风险：需要兼容首次部署没有 last-good 的场景。

涉及文件：

- `.github/workflows/docker-deploy.yml`
- `deploy/git-deploy.sh`
- `deploy/compose.prod.yaml`
- `README.md`

验证方式：

- deploy summary 显示 new digest 和 previous digest。
- 健康检查失败时回滚到 previous digest。
- `.env data` 不变。

预计工作量：1.5 天。

### 1.4 Trivy 镜像扫描

目标：HIGH/CRITICAL 镜像漏洞阻断发布。

收益：供应链风险可见。

风险：首次启用可能暴露基础镜像漏洞，需要允许临时 allowlist。

涉及文件：

- `.github/workflows/docker-deploy.yml`
- `.github/dependabot.yml` 或 Renovate 配置。

验证方式：

- Actions 输出 Trivy summary。
- SARIF 上传或 artifact 存档。

预计工作量：0.5 天。

### 1.5 轻量 health endpoint

目标：Dockerfile、compose、deploy 统一探 `/api/health`。

收益：首页 SSR 或内容问题不会误判进程健康。

风险：端点设计不能访问敏感数据。

涉及文件：

- `src/app/api/health/route.ts`
- `Dockerfile`
- `deploy/compose.prod.yaml`
- `.github/workflows/docker-deploy.yml`

验证方式：

- 单元测试 health 返回 200。
- Docker healthcheck 通过。

预计工作量：0.5 天。

## 第 2 阶段：体验优化

### 2.1 修复 warning 和 hover 对比度

目标：小字号状态 badge 和代码复制按钮 hover 可读。

收益：立即改善后台和文章阅读可用性。

风险：视觉微调需检查深色模式。

涉及文件：

- `src/app/styles/design-tokens.css`
- `tailwind.config.ts`
- `src/app/styles/markdown-preview.css`
- `src/app/editor/(authenticated)/blog/new/ArticleEditorPanels.tsx`
- `src/app/editor/(authenticated)/blog/new/NewArticleContent.tsx`

验证方式：

- token 对比度测试。
- UI smoke 截图。

预计工作量：0.5 天。

### 2.2 统一 Field 组件和错误语义

目标：设置页、运行时配置、R2 配置、导航编辑表单统一 `aria-invalid`、`aria-describedby`、错误聚焦。

收益：减少重复播报，提高键盘和读屏体验。

风险：触及表单较多，需分页面迁移。

涉及文件：

- `src/app/components/ui/Field.tsx` 或同类新组件
- `src/app/editor/(authenticated)/settings/runtime/page.tsx`
- `src/app/editor/(authenticated)/settings/CloudflareR2SettingsPanel.tsx`
- `src/app/editor/(authenticated)/settings/page.tsx`
- `src/app/editor/(authenticated)/navigation/page.tsx`

验证方式：

- 组件测试断言 aria。
- UI smoke 断言错误焦点。

预计工作量：2 天。

### 2.3 Loading 和 focus 语义

目标：loading 有 `role=status`，程序化定位有可见 focus。

收益：可访问性基础补齐。

风险：低。

涉及文件：

- `src/app/loading.tsx`
- `src/app/editor/(authenticated)/blog/new/NewArticleContent.tsx`
- `src/app/editor/(authenticated)/blog/components/PreviewPane.tsx`
- `src/app/editor/(authenticated)/settings/runtime/page.tsx`

验证方式：

- `@testing-library/react` 查询 role。
- mobile smoke 检查焦点。

预计工作量：1 天。

### 2.4 文章工作流视图

目标：从单纯列表按钮升级为“草稿 / 待修复 / 可发布 / 已发布”视图。

收益：后台编辑效率提升，发布前质量检查更明确。

风险：状态计算需要与服务端 blocking check 保持一致。

涉及文件：

- `src/app/editor/(authenticated)/blog/page.tsx`
- `src/lib/article-quality.ts`
- `tests/app/editor-blog-page.test.tsx`
- `scripts/test/verify-editor-blog-ui.py`

验证方式：

- E2E：创建草稿、修复 blocking、发布、回草稿。

预计工作量：2 天。

### 2.5 R2 操作忙碌态

目标：同步、恢复、bootstrap 显示具体 busy action。

收益：减少后台误操作和不确定感。

风险：低。

涉及文件：

- `src/app/editor/(authenticated)/settings/CloudflareR2SettingsPanel.tsx`

验证方式：

- 组件测试：点击同步按钮显示“同步中”。
- 组件测试：点击恢复按钮显示“恢复中”。

预计工作量：0.5 天。

## 第 3 阶段：架构优化

### 3.1 R2 pending failed 状态和手动重试

目标：失败任务不再静默丢弃。

收益：远端备份可靠性和可观测性提高。

风险：需要兼容旧 `.backup-pending.json`。

涉及文件：

- `src/lib/backup-coordinator.ts`
- `src/lib/editor-remote-backup.ts`
- `src/app/api/data/backup/remote/*`
- `src/app/editor/(authenticated)/settings/CloudflareR2SettingsPanel.tsx`
- `tests/lib/backup-coordinator.test.ts`

验证方式：

- 失败 3 次进入 failed。
- 手动重试后成功清除 failed。
- 设置页展示失败原因。

预计工作量：2 天。

### 3.2 snapshot 绑定 manifest/hash

目标：snapshot 语义准确。

收益：恢复和审计更可信。

风险：需要调整 backup payload version。

涉及文件：

- `src/lib/editor-data-backup.ts`
- `src/lib/editor-remote-backup.ts`
- `src/lib/r2-backup-storage.ts`
- `tests/lib/r2-backup-storage.test.ts`

验证方式：

- 保存时 snapshot key 含 manifest hash。
- 延迟 drain 时不伪装为旧状态。

预计工作量：1.5 天。

### 3.3 manifest transaction

目标：资源 JSON 与 manifest 事务一致。

收益：消除核心数据一致性风险。

风险：存储层改动较深，必须有失败注入测试。

涉及文件：

- `src/lib/editor-data-storage.ts`
- `src/lib/editor-data-lock.ts`
- 新增 `src/lib/manifest-transaction.ts`
- 新增 `src/lib/atomic-json-writer.ts`

验证方式：

- 资源写失败。
- manifest 写失败。
- rename 失败。
- 重启恢复 marker。

预计工作量：3 天。

### 3.4 配置写入 fsync 和锁

目标：R2 配置、运行时配置与主数据写入安全级别一致。

收益：配置损坏风险降低。

风险：路径兼容。

涉及文件：

- `src/lib/r2-backup-storage.ts`
- `src/lib/app-runtime-config.ts`
- `src/lib/runtime-config.ts`

验证方式：

- 配置写失败不留下半文件。
- 恢复期间不能并发改配置。

预计工作量：1 天。

### 3.5 schemaVersion 和迁移 registry

目标：为后续数据拆分和结构升级准备。

收益：备份恢复跨版本可控。

风险：旧数据兼容必须谨慎。

涉及文件：

- `src/lib/editor-data-storage.ts`
- `src/lib/editor-data-backup.ts`
- `data/manifest.json` 生成逻辑
- `tests/scripts/data-migration-scripts.test.ts`

验证方式：

- 旧 manifest 可读。
- 新 manifest 写 schemaVersion。
- backup payload 包含 schemaVersion。

预计工作量：2 天。

## 第 4 阶段：长期演进

### 4.1 文章分文件存储

目标：从单体 `articles.json` 演进到 `articles/index.json + articles/by-slug/*.json`。

收益：降低整包写入、hash、R2 payload 压力。

风险：迁移复杂，导入导出和恢复要兼容。

涉及文件：

- `src/lib/editor-data-storage.ts`
- `src/lib/markdown.ts`
- `src/lib/editor-data-backup.ts`
- `scripts/data/runtime-data.mjs`

验证方式：

- 旧单体数据迁移。
- 新分文件读写。
- R2 latest 仍是明文 JSON。
- 恢复后 hash 一致。

预计工作量：5 天。

### 4.2 轻量权限模型

目标：从单编辑口令演进到 admin/editor 两级能力。

收益：为多人协作预留路径。

风险：认证和 session 改动敏感。

涉及文件：

- `src/lib/editor-auth-runtime.ts`
- `src/lib/editor-api-auth.ts`
- `src/app/editor/*`

验证方式：

- admin 可改设置。
- editor 可改文章/导航。
- 未授权 API 403。

预计工作量：3 天。

### 4.3 schema 驱动字段

目标：文章、导航、设置字段定义集中。

收益：减少表单和数据模型漂移。

风险：过度抽象。

涉及文件：

- `src/lib/resource-schema/*`
- 编辑器表单组件。

验证方式：

- schema 单测。
- 表单默认值和错误来自 schema。

预计工作量：4 天。

### 4.4 R2 snapshot 保留策略

目标：长期启用 snapshot 不无限增长。

收益：控制 R2 成本和对象数量。

风险：删除策略必须谨慎。

涉及文件：

- `src/lib/r2-backup-storage.ts`
- `src/app/api/data/backup/remote/*`

验证方式：

- list snapshot。
- dry-run 删除。
- 保留最近 N 份和最近 N 天。

预计工作量：2 天。

## 推荐实现顺序

1. Docker 更新行为级测试。
2. Docker 同源镜像 smoke + volume smoke。
3. digest 部署 + `.last-good-digest`。
4. Trivy。
5. UX P1：对比度、runtime 表单错误、loading。
6. R2 failed 队列。
7. snapshot 绑定 manifest/hash。
8. manifest transaction。
9. schemaVersion。
10. 文章分文件存储。

## 每次实施后的共同验证

```bash
npm run lint -- --fix
npm run typecheck
npm run test:coverage
npm run test:architecture
git diff --check
```

涉及 Docker/GHCR 的变更优先推 GitHub Actions 验证；本地不要构建发布镜像。

## 2026-06-08 多 Agent 最新路线图

本节把全部 Agent 结论压缩成可执行任务顺序。默认后续 Codex 按阶段推进，每一项都必须给出涉及文件和验证方式。

### 第 0 阶段：立即修复

#### 0.1 固化 R2 明文自动备份

目标：任何 R2 自动备份改动都不能新增远端口令字段，上传体必须可直接 `JSON.parse`。

收益：避免灾备不可读、恢复复杂化。

风险：无业务风险。

涉及文件：`AGENTS.md`、`README.md`、`src/lib/r2-backup-storage.ts`、`tests/lib/r2-backup-storage.test.ts`、`tests/architecture/repository-structure.test.ts`。

验证方式：用仓库既有架构测试和人工搜索检查旧远端口令相关模式无新增命中；`npm run test:architecture`；相关 R2 测试。

预计工作量：0.5 天。

#### 0.2 固化 Docker 更新保留 `.env data`

目标：更新镜像不覆盖生产 `.env` 和宿主机 `data/`。

收益：用户不再每次更新后重新配置。

风险：若自动同步 compose，需避免覆盖服务器配置。

涉及文件：`deploy/git-deploy.sh`、`.github/workflows/docker-deploy.yml`、`compose.yaml`、`deploy/compose.prod.yaml`、`README.md`。

验证方式：architecture test、docker-update 行为级测试、workflow smoke。

预计工作量：0.5-1 天。

#### 0.3 修复关键可访问性

目标：修 token 对比度、局部 loading 语义、危险恢复确认。

收益：提升后台可用性，降低误恢复风险。

风险：视觉变化需要截图确认。

涉及文件：`src/app/styles/design-tokens.css`、`tailwind.config.ts`、`src/app/blog/loading.tsx`、`src/app/navigation/loading.tsx`、`src/app/posts/[...slug]/loading.tsx`、`src/app/editor/(authenticated)/page.tsx`、`src/app/editor/(authenticated)/settings/CloudflareR2SettingsPanel.tsx`。

验证方式：组件测试、UI smoke、Playwright 截图。

预计工作量：1-2 天。

### 第 1 阶段：发布前修复

#### 1.1 扫描最终 GHCR digest

目标：Trivy 扫描最终发布镜像，而不是只扫本地 smoke image。

收益：发布门禁可信。

风险：workflow 改造需调试 digest 传递。

涉及文件：`.github/workflows/docker-deploy.yml`。

验证方式：GitHub Actions run summary 显示 digest、Trivy target、SARIF artifact/security tab。

预计工作量：1 天。

#### 1.2 固定基础镜像版本和 BuildKit cache

目标：减少不可复现构建，加速 `npm ci`。

收益：稳定 CI 和漏洞扫描结果。

风险：Node patch 版本升级需定期维护。

涉及文件：`Dockerfile`、`.github/workflows/docker-deploy.yml`。

验证方式：GitHub Actions Docker build、Trivy、smoke。

预计工作量：0.5-1 天。

#### 1.3 备份队列加锁、failed 状态、损坏隔离

目标：队列可诊断、可恢复，不因坏 JSON 卡死。

收益：降低远端备份长期落后风险。

风险：需要处理旧 `.backup-pending.json` 兼容。

涉及文件：`src/lib/backup-coordinator.ts`、`src/lib/editor-remote-backup.ts`、`src/app/editor/(authenticated)/settings/CloudflareR2SettingsPanel.tsx`、`tests/lib/backup-coordinator.test.ts`。

验证方式：损坏队列测试、failed retry 测试、设置页状态测试。

预计工作量：1-2 天。

#### 1.4 本地恢复前持久化快照

目标：本地导入恢复前也保留 `pre-local-restore`。

收益：恢复误操作可回退。

风险：备份文件增长，需要保留策略。

涉及文件：`src/app/api/data/backup/route.ts`、`src/lib/editor-data-backup.ts`、`src/lib/editor-data-storage.ts`、`tests/app/backup-route.test.ts`。

验证方式：restore API 测试断言 snapshot id 和文件存在。

预计工作量：1 天。

### 第 2 阶段：体验优化

#### 2.1 R2 设置页状态优先重组

目标：先展示启用状态、latest/snapshot、failed queue、风险等级。

收益：用户能判断是否有远端灾备。

风险：组件拆分需保持现有测试稳定。

涉及文件：`src/app/editor/(authenticated)/settings/CloudflareR2SettingsPanel.tsx`、`tests/app/editor-settings-page.test.tsx`、`scripts/test/verify-editor-settings-ui.py`。

验证方式：组件测试 + UI smoke。

预计工作量：1-2 天。

#### 2.2 后台导航搜索/筛选和文章排序/批量操作

目标：提升大量内容管理效率。

收益：减少逐条滚动编辑。

风险：批量操作必须有确认和备份状态提示。

涉及文件：`src/app/editor/(authenticated)/navigation/page.tsx`、`src/app/editor/(authenticated)/blog/page.tsx`、相关 tests。

验证方式：组件测试、UI smoke。

预计工作量：2-3 天。

#### 2.3 Markdown 编辑器发布侧栏和移动端底部操作栏

目标：保存、预览、质量检查、发布状态始终可见。

收益：接近 Outstatic/Payload 的高效后台，但保持轻量。

风险：移动端布局需截图验证。

涉及文件：`src/app/editor/(authenticated)/blog/new/NewArticleContent.tsx`、`ArticleEditorPanels.tsx`、`MarkdownEditor.tsx`。

验证方式：创建草稿、保存、预览、发布 smoke。

预计工作量：2-4 天。

### 第 3 阶段：架构优化

#### 3.1 引入 schema 与 migration registry

目标：为 articles/navigation/settings/manifest/backup payload 定义 schema 和迁移链。

收益：后续结构演进有门禁。

风险：一次性迁移过大，需先只读校验。

涉及文件：`src/lib/editor-resource-schema.ts`、`scripts/data/migrations/`、`tests/lib/*schema*.test.ts`。

验证方式：旧版本导入、未来版本拒绝、迁移后 hash 校验。

预计工作量：2-4 天。

#### 3.2 manifest 审计字段

目标：增加 `previousHash/committedAt/reason/schemaVersion`。

收益：恢复和诊断更可靠。

风险：兼容旧 manifest。

涉及文件：`src/lib/editor-data-storage.ts`、`src/lib/manifest-transaction.ts`、`tests/lib/editor-data-storage.test.ts`。

验证方式：旧 manifest 自动补默认字段，恢复后校验。

预计工作量：2 天。

#### 3.3 抽 ResourceService

目标：让 API 不直接依赖大而全的存储文件。

收益：模块边界清晰，便于草稿/发布双轨。

风险：重构范围大，需要小步迁移。

涉及文件：`src/lib/editor-data-storage.ts`、`src/lib/editor-resource-service.ts`、`src/app/api/data/*`。

验证方式：全量 API 测试、coverage、architecture。

预计工作量：3-5 天。

### 第 4 阶段：长期演进

#### 4.1 最小草稿/发布双轨

目标：保存草稿不影响公开 published 内容。

收益：发布更安全。

风险：数据模型和 UI 都要变，需迁移。

涉及文件：`src/app/types/article.ts`、`src/lib/editor-data-storage.ts`、`src/lib/markdown.ts`、编辑器页面。

验证方式：草稿不可公开、发布后公开、回草稿后公开隐藏。

预计工作量：4-7 天。

#### 4.2 文章分文件存储

目标：从单 `articles.json` 演进为 `articles/index.json + articles/by-slug/*.json`。

收益：大文章集性能和恢复粒度更好。

风险：迁移和 R2 payload 兼容复杂。

涉及文件：`src/lib/editor-data-storage.ts`、`src/lib/editor-data-backup.ts`、`scripts/data/*`。

验证方式：导入导出兼容、R2 latest 明文聚合、分文件 hash。

预计工作量：5-8 天。

#### 4.3 docs 信息架构重组

目标：把文档拆成入门、使用、运维、开发、架构、设计系统。

收益：后续用户和 Codex 都更容易定位。

风险：链接迁移需保留索引。

涉及文件：`README.md`、`docs/优化文档索引.md`、`docs/入门/*`、`docs/使用/*`、`docs/运维/*`、`docs/开发/*`、`docs/架构/*`。

验证方式：`rg` 检查旧链接、README 最短部署路径仍可用。

预计工作量：2-4 天。

### 推荐实施顺序

1. R2 明文和 Docker 保留数据护栏。
2. token/loading/恢复 modal。
3. Trivy final digest + SARIF。
4. 备份队列 failed/lock/corrupt。
5. 本地恢复前快照。
6. runtime 设置页测试和 smoke。
7. R2 设置页状态重组。
8. schema/migration registry。
9. manifest 审计字段。
10. 最小草稿/发布双轨。
