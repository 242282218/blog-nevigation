# 测试与质量体系优化方案

生成日期：2026-06-07
目标：把当前已有测试体系从“功能覆盖较多”推进到“发布、数据保留、恢复、R2 明文、UX 可访问性都有可执行门禁”。

## 当前测试覆盖映射

当前仓库约 49 个测试文件、约 366 个 `it(...)` 用例。coverage 快照约 statements 66.96%、functions 67.29%、branches 59.30%。脚本入口证据：`package.json:27`、`vitest.config.ts:17`。

| 功能域 | 已覆盖 | 证据 |
|---|---|---|
| 基础门禁 | lint、typecheck、coverage、architecture、UI smoke | `package.json:24`、`package.json:27`、`vitest.config.ts:17` |
| 架构边界 | src 迁移、旧根目录约束、Docker/部署字符串约束 | `tests/architecture/repository-structure.test.ts:11`、`tests/architecture/repository-structure.test.ts:173` |
| R2 明文备份 | 上传体可读 JSON、下载后恢复、状态提示 | `tests/lib/r2-backup-storage.test.ts:381`、`src/lib/r2-backup-storage.ts:476` |
| 防历史字段回归 | setup/runtime/bootstrap 不保存历史口令类字段 | `tests/app/setup-route.test.ts:138`、`tests/app/cloudflare-r2-route.test.ts:468` |
| R2 配置/恢复 | 配置校验、密钥不回显、恢复前快照失败保护 | `tests/app/cloudflare-r2-route.test.ts:356`、`tests/app/remote-backup-route.test.ts:317` |
| 编辑器数据 API | CSRF/origin、坏 JSON、超大 body、revision 冲突、备份入队 | `tests/app/editor-data-routes.test.ts:96`、`tests/app/editor-data-routes.test.ts:302` |
| 备份队列 | pending 持久化、drain、失败重试、启动定时 | `tests/lib/backup-coordinator.test.ts:45`、`tests/lib/startup-tasks.test.ts:36` |
| 数据迁移脚本 | export/import/verify、错误导入不覆盖、失败回滚 | `tests/scripts/data-migration-scripts.test.ts:77`、`tests/scripts/data-migration-scripts.test.ts:467` |
| UI smoke | 公开页、编辑登录、首页、导航、博客、设置、移动端触达 | `scripts/test/verify-public-ui.py:73`、`scripts/test/verify-editor-blog-ui.py:124` |
| Docker 数据保留 | 静态断言 bind mount 和 `.env data` | `compose.yaml:23`、`deploy/git-deploy.sh:115`、`tests/architecture/repository-structure.test.ts:205` |

## 测试缺口

### P1：Docker 更新缺少行为级测试

现有测试主要是字符串断言，不能证明更新脚本真的保留 `.env` 和 `data/`。证据：`deploy/git-deploy.sh:115`、`tests/architecture/repository-structure.test.ts:173`、`tests/architecture/repository-structure.test.ts:205`。

应补：

- `tests/scripts/docker-update.test.ts`
- 用临时 `DEPLOY_PATH`。
- fake `docker`、`git`、`tar` 放 PATH 前面。
- 执行 `deploy/git-deploy.sh`。
- 断言 `.env` 未被覆盖、`data/` 未被删除、tar 参数包含 `.env data`、compose 仍是 bind mount。

### P1：Docker smoke 没挂载真实数据目录

CI smoke 当前 `docker run` 只传 env，缺少 `-v <temp>:/var/lib/blog-navigation`，不能发现权限、entrypoint、持久化问题。证据：`.github/workflows/docker-deploy.yml:77`。

应补：

- 创建 `.tmp/docker-smoke-data`。
- `docker run -v "$PWD/.tmp/docker-smoke-data:/var/lib/blog-navigation"`。
- 容器启动后写入一个 runtime 文件。
- 重建容器后确认文件仍在。

### P1：R2 外部协议全是 mock

单元覆盖细，但 Cloudflare/S3 真实 Body 类型、403/404/NoSuchKey、权限返回 shape 变化无法发现。

应补：

- 可选本地 S3 兼容服务契约测试。
- 或 nightly 真实 R2 测试，必须使用测试 bucket。
- 覆盖 PutObject/GetObject ContentType、NoSuchKey、超限对象。

### P1：manifest 事务失败注入缺失

当前没有测试“资源写成功但 manifest 写失败”的一致性结果。

应补：

- mock `writeManifest` 抛错。
- 断言不会半提交，或启动恢复 marker 可诊断。
- 测试 articles/navigation/settings 三类资源。

### P2：UI 可访问性缺口

缺颜色对比、runtime 表单错误、loading role、预览目录、移动端可见 focus 测试。

证据：`tailwind.config.ts:58`、`src/app/styles/markdown-preview.css:194`、`src/app/editor/(authenticated)/settings/runtime/page.tsx:214`、`src/app/loading.tsx:3`、`src/app/editor/(authenticated)/blog/components/PreviewPane.tsx:103`。

应补：

- token 对比度测试。
- runtime 表单错误 aria 测试。
- loading `role=status` 测试。
- PreviewPane TOC `aria-label` 和 focus class 测试。
- 移动端错误定位可见 focus 测试。

### P2：公开输出 0 覆盖

`robots.ts`、`sitemap.ts`、`og/route.tsx`、`api/favicon/route.ts` 等公开输出应补轻量测试。

应补：

- URL 正确。
- content-type 正确。
- 缓存头正确。
- site URL 配置错误时有合理 fallback。

## Mock 风险

- `remote-backup-route.test.ts` mock 掉 R2 下载/同步，只验证路由编排，不验证真实上传下载联动。
- `editor-data-routes.test.ts` mock 掉 queue，只验证“调用入队”，不验证 pending 写失败对 API 的影响。
- UI smoke 部分关键列表路径用 route fulfill 和 localStorage，降低服务端持久化合同覆盖。证据：`scripts/test/verify-editor-blog-ui.py:101`、`scripts/test/verify-editor-blog-ui.py:319`。

缓解：

- 保留单元 mock，但补一层契约测试。
- UI smoke 增加“真实 API 模式”和“离线兜底模式”两个路径。
- 对 R2 明文、Docker 保数据这类硬约束，优先行为测试，不只静态断言。

## 应补单元测试

### 数据层

1. `manifest-transaction.test.ts`
   - stage 成功 commit。
   - resource 写失败不改 manifest。
   - manifest 写失败保留 marker。
   - 启动恢复 marker。

2. `atomic-json-writer.test.ts`
   - temp + rename。
   - fsync 调用。
   - 临时文件失败清理。
   - 同一路径写入串行。

3. `backup-coordinator.test.ts`
   - latest 去重。
   - snapshot 绑定 manifest hash。
   - 失败任务进入 failed。
   - 手动 retry。
   - 队列文件锁。

4. `r2-backup-storage.test.ts`
   - payload 包含 schemaVersion。
   - payload 明文可读。
   - ContentType JSON。
   - R2 返回损坏 JSON 时不覆盖本地。

### API 层

1. `remote-backup-status-route.test.ts`
   - 展示 failed 队列。
   - 展示 latest key、snapshot key。
   - 不返回 secret。

2. `data-restore-route.test.ts`
   - 恢复前 snapshot 失败时拒绝覆盖。
   - manifest mismatch 返回 409。
   - 恢复成功后重算 hash。

3. `runtime-config-route.test.ts`
   - `pendingPath` 只表示待切换。
   - 不误报当前生效路径。

## 应补集成测试

### Docker 更新模拟

测试名：`tests/scripts/docker-update.test.ts`

流程：

1. 创建临时 deploy 目录。
2. 写入 `.env` 和 `data/settings/site.json`。
3. fake `docker` 记录命令但不真实执行。
4. fake `git` 返回固定 commit。
5. fake `tar` 记录打包参数。
6. 执行 deploy 脚本。
7. 断言 `.env` 内容未变。
8. 断言 `data/settings/site.json` 未变。
9. 断言打包包含 `.env data`。
10. 断言没有 `rm -rf data` 行为。

### R2 契约测试

模式：

- 默认跳过。
- 设置 `RUN_R2_CONTRACT_TESTS=true` 时运行。
- 使用测试 bucket 和测试 prefix。
- 测试结束清理测试 prefix。

断言：

- PutObject body 是明文 JSON。
- GetObject 后 parse 成相同 payload。
- 404/NoSuchKey 变成可诊断错误。
- 权限不足不写本地。

### 数据恢复集成

流程：

1. 临时数据根写入文章/导航/设置。
2. 生成 backup payload。
3. 修改本地数据。
4. 恢复旧 payload。
5. 校验恢复前 snapshot 存在。
6. 校验 manifest hash。
7. 校验 R2 latest 入队。

## 应补 UI smoke

### 公开页

- 首页无横向溢出。
- 博客详情长 URL 不撑开。
- TOC focus 可见。
- 代码块复制 hover 可读。

### 编辑器

- runtime 口令不一致：字段错误 aria、焦点。
- 新文章 blocking：点击检查定位到可见焦点。
- PreviewPane TOC：`aria-label`、键盘可聚焦。
- R2 同步：按钮显示“同步中”。
- R2 恢复：按钮显示“恢复中”。

### Docker UI smoke

- 用最终镜像启动。
- 挂载临时数据目录。
- 完成登录、设置页、文章列表加载。
- 重启容器后数据仍存在。

## 应补 Docker smoke

推荐 workflow 步骤：

1. Buildx `load: true` 构建 `blog-navigation:ci-smoke-${sha}`。
2. 创建 `.tmp/docker-smoke-data`。
3. `docker run -v "$PWD/.tmp/docker-smoke-data:/var/lib/blog-navigation"`。
4. health endpoint 通过。
5. 写入一个 marker 文件或通过 API 写入设置。
6. 停止并重启容器。
7. 验证 marker/设置仍在。
8. Trivy 扫描同一镜像。
9. 扫描通过再 push。

## CI 阶段设计

### PR

- checkout
- npm ci
- npm audit
- lint
- typecheck
- unit + architecture
- coverage
- public/editor UI smoke
- Docker build load + smoke（可按路径触发）

### main

- PR 全量门禁。
- Docker build load。
- Docker smoke with volume。
- Trivy image scan。
- push GHCR tags。
- generate digest summary。

### tag

- main 全量门禁。
- semver tag。
- attestation。
- release notes。

### manual deploy

- 只允许 digest 部署。
- 同步/校验 compose。
- 备份 `.env data`。
- health check。
- 失败只回滚镜像。
- 写 `.last-good-digest`。

## 覆盖率目标

当前覆盖率不要一次性大幅提高，避免大量无意义测试。建议：

### 第 0 阶段

- statements 65%
- functions 65%
- branches 58%
- lines 65%

目标：先防止回落。

### 第 1 阶段

- statements 70%
- functions 70%
- branches 62%
- lines 70%

目标：补存储、备份、Docker 更新测试。

### 第 2 阶段

- statements 75%
- functions 75%
- branches 68%
- lines 75%

目标：补 UI a11y、恢复、公开输出。

### 模块特殊目标

- `src/lib/*backup*`：85%+
- `src/lib/editor-data-*`：80%+
- `src/app/api/data/*`：80%+
- `scripts/data/*`：75%+

## 验证命令

```bash
npm run lint -- --fix
npm run typecheck
npm run test:coverage
npm run test:architecture
python scripts/test/verify-public-ui.py
python scripts/test/verify-editor-blog-ui.py
```

Docker/GHCR 相关验证优先在 GitHub Actions 跑，不在本地构建发布镜像。

## 2026-06-08 多 Agent 测试体系增补

本节把当前测试审查和外部项目测试实践转成后续可执行清单。

### 当前覆盖复核

| 功能域 | 已有覆盖 | 证据 |
|---|---|---|
| 鉴权/CSRF/限流 | 登录、初始化、cookie、session、跨源、CSRF | `tests/app/editor-auth.test.ts:133`、`tests/app/csrf-protection.test.ts:52`、`tests/app/editor-middleware.test.ts:44` |
| 数据 API | articles/navigation/settings 路由、坏 JSON、超大 body、revision 冲突 | `tests/app/editor-data-routes.test.ts:96`、`:242`、`:271` |
| 本地数据/恢复 | 存储事务、manifest、恢复残留、fsync | `tests/lib/editor-data-storage.test.ts:85`、`:454` |
| R2 明文 | 上传体 JSON、snapshot key、下载恢复 | `tests/lib/r2-backup-storage.test.ts:363`、`:436`、`:513` |
| 远端恢复 | sync/restore/retry/currentManifest | `tests/app/remote-backup-route.test.ts:130`、`:700` |
| 编辑器 UI | blog/navigation/settings/editor/preview/hook | `tests/app/editor-blog-page.test.tsx:47`、`tests/app/editor-navigation-page.test.tsx:60`、`tests/app/editor-settings-page.test.tsx:103` |
| Public UI smoke | public、editor login/home/navigation/blog/settings | `scripts/test/verify-public-ui.py:73`、`scripts/test/verify-editor-settings-ui.py:62` |
| 架构规则 | Docker、R2 明文、历史字段防回归、目录边界 | `tests/architecture/repository-structure.test.ts:183`、`:250` |

### 当前测试缺口

1. `src/app/editor/(authenticated)/settings/runtime/page.tsx` 只测口令确认不一致，缺成功保存、失败响应、CSRF header、`requiresRestart`。证据：`src/app/editor/(authenticated)/settings/runtime/page.tsx:176`、`:250`、`tests/app/runtime-settings-page.test.tsx:61`。
2. SEO/资源路由缺直接测试：`src/app/api/favicon/route.ts:24`、`src/app/og/route.tsx:14`、`src/app/sitemap.ts:11`、`src/app/robots.ts:4`。
3. `src/lib/api-json-body.ts` 缺无 `Content-Length` 分块超限、非法 JSON、空 body、异常 header 单测。证据：`src/lib/api-json-body.ts:52`、`:82`。
4. `src/instrumentation.ts` 缺 `NEXT_RUNTIME !== nodejs` 和 `nodejs` 动态导入分支测试。证据：`src/instrumentation.ts:1`、`:8`。
5. `ReadingProgress`、`TableOfContents` 覆盖不足。证据：`src/app/components/ui/ReadingProgress.tsx:18`、`src/app/components/ui/TableOfContents.tsx:15`、`src/app/posts/[...slug]/page.tsx:206`。
6. UI smoke 未覆盖 `/editor/settings/runtime`，证据：`scripts/test/verify-editor-settings-ui.py:62`、`:99`。
7. R2 路由测试 mock 较多，缺“route -> uploadBackupPayloadToR2 -> fake S3 client -> 明文体”较少 mock 集成测试。证据：`tests/app/remote-backup-route.test.ts:27`、`tests/lib/r2-backup-storage.test.ts:37`。

### 应补单元测试

| 文件 | 测试点 |
|---|---|
| `tests/lib/api-json-body.test.ts` | 空 body、空白 body、无 Content-Length 分块超限、非法 JSON、负数/非数字 Content-Length。 |
| `tests/lib/backup-coordinator.test.ts` | 队列锁、损坏队列隔离、failed 状态保留、手动 retry。 |
| `tests/lib/editor-data-storage.test.ts` | 恢复后目录 fsync、committed 状态 manifest/hash 校验失败路径。 |
| `tests/lib/r2-backup-storage.test.ts` | latest/snapshot 明文、snapshot key 带 manifest/hash、下载未来 schemaVersion 拒绝。 |
| `tests/app/runtime-settings-page.test.tsx` | 保存成功、保存失败、CSRF header、重启提示、空口令只保存配置。 |
| `tests/app/reading-progress.test.tsx` | scroll 进度、cleanup、无 document 容错。 |
| `tests/app/table-of-contents.test.tsx` | 空 headings 返回 null、点击锚点、active 状态。 |

### 应补集成测试

1. 本地恢复前快照：调用本地 restore API，断言生成 `pre-local-restore` 明文快照。
2. R2 明文端到端：route 调用真实 `uploadBackupPayloadToR2`，只 fake S3 client，断言 body 可直接 `JSON.parse`。
3. 队列损坏恢复：写入坏 `.backup-pending.json`，触发 status/drain，断言隔离文件和高危状态。
4. Docker 更新脚本行为级测试：fake `docker/git/tar`，断言 `.env` 和 `data/` 不被覆盖。
5. schema migration 演练：旧 `schemaVersion` 导入 -> migration -> manifest 校验 -> R2 payload 明文。

### 应补 UI smoke

新增脚本：

- `scripts/test/verify-editor-runtime-settings-ui.py`
- `scripts/test/verify-editor-restore-flow-ui.py`
- `scripts/test/verify-editor-r2-risk-state-ui.py`

覆盖点：

- `/editor/settings/runtime` 可加载、保存按钮状态、错误语义、移动端无横向溢出。
- R2 disabled/failed 状态在后台可见。
- 恢复 modal 展示来源、时间、manifest、影响范围、二次确认。
- public `/blog`、`/navigation`、`/posts/*` loading 和 focus 不报明显 console error。

### 应补 Docker smoke

当前 Docker smoke 已验证挂载文件重启后保留，证据：`.github/workflows/docker-deploy.yml:125`、`:131`。后续补强：

1. smoke 容器必须挂载临时 data root。
2. smoke 写 marker 后 restart，验证 marker 仍存在。
3. smoke 探 `/`、`/blog`、`/navigation`、`/api/search?q=test`。
4. 如设置 `EDITOR_LOGIN_SECRET`，执行轻量登录页探针。
5. Trivy 扫描最终 digest。

### CI 阶段设计

```text
validate
  -> check-env
  -> lint
  -> typecheck
  -> unit/component/api test
  -> architecture test
  -> coverage gate
build
  -> next build
  -> docker build smoke image
  -> docker smoke with mounted data
security
  -> Trivy image/fs/config
  -> upload SARIF
publish
  -> push GHCR digest
  -> scan final digest
deploy
  -> backup .env data
  -> deploy digest
  -> health / /blog /navigation
  -> record .last-good-digest
```

### 覆盖率目标

| 目录/模块 | 当前问题 | 目标 |
|---|---|---|
| 全局 | 仅全局阈值，核心 0 覆盖也能通过 | statements 75%、branches 65%、functions 70%、lines 75%。 |
| `src/lib/*backup*` | 核心灾备模块 | 85%+。 |
| `src/lib/editor-data-*` | 数据一致性核心 | 80%+，并有失败注入。 |
| `src/app/api/data/*` | 对外写入口 | 80%+，覆盖错误路径。 |
| `scripts/data/*` | 运维脚本 | 75%+，覆盖不覆盖数据。 |
| `src/app/api/favicon/og/sitemap/robots` | 当前缺覆盖 | 每个至少主路径 + 错误/默认路径。 |

### 外部实践取舍

- 借 Tina/Payload 的 E2E 发布前门禁，但不照搬大矩阵。
- 借 Starlight 的 CI 分层和 size/format 思路，但不引入 npm release 链。
- 借 Trivy Action 的 SARIF 和 scan-type，但不默认忽略 unfixed。
- 借 Decap 的 Cypress/E2E 思路，但当前继续用 Python Playwright smoke。
